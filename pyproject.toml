[project]
name = "creator-wiki"
version = "0.1.0"
description = "Documentation for the CREATOR assembly simulator"
requires-python = ">=3.11"
dependencies = [
    "zensical>=0.0.57",
]

[tool.uv]
package = false
